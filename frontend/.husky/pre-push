#!/usr/bin/env sh
racine=$(git rev-parse --show-toplevel)
export MSYS_NO_PATHCONV=1

docker run --rm \
  -v "$racine/frontend:/app" \
  -v solida_frontend_node_modules:/app/node_modules \
  -w /app \
  node:22 \
  sh -c "npm install --no-audit --no-fund --silent && npx tsc --noEmit"
